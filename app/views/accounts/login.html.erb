<%= notice %>

<h1> login </h1>

<%= form_with url: "/login" do |form| %>
	Username: <p>
	<%= form.text_field :username %> <p>
	Password: <p>
	<%= form.password_field :password %> <p>
	<%= form.submit "login" %>
<% end %>

<a href="/accounts/new">  <button> Register </button> </a>